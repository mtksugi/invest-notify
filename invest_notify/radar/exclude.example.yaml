# Radar ユニバースから「永久に」除外したい銘柄
# build-universe 時に毎回参照される。
# `data/radar/exclude.yaml` にコピーして編集してください（gitignore 対象）。

exclude:
  # - ticker: SPAC1
  #   reason: SPAC 残存
  # - ticker: PENNYX
  #   reason: ペニー化 / 流動性なし
